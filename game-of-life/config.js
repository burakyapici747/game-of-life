const gameSettings = {};

gameSettings.canvasSettings = {
    canvasWidth: 800,
    canvasHeight: 800,
    resolution: 8,
    refreshTime: 50,
};

gameSettings.canvasObjectSettings = {
    cellColor: "black",
};